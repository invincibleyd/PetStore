Petstore v4 - Adoption workflow (Bootstrap + Tailwind UI)

Run:
1) unzip and cd into petstore_v4
2) mvn clean install
3) mvn spring-boot:run

Admin actions require header X-ADMIN-KEY with value: secret-admin-key

Open http://localhost:8080 to use UI
